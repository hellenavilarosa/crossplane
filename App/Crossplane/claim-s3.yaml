apiVersion: s3.aws.upbound.io/v1alpha1
kind: Bucket
metadata:
  name: my-bucket
spec:
  forProvider:
    bucketName: crossplane-my-bucket-name
    acl: private
    versioning:
      enabled: true
  providerConfigRef:
    name: default

